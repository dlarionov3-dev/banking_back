services:
  postgres:
    image: postgres:16-alpine
    container_name: postgres
    restart: no
    environment:
      POSTGRES_DB: virtualthreads
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5431:5432"